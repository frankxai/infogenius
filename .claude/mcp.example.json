{
  "mcpServers": {
    "nano-banana": {
      "command": "npx",
      "args": ["-y", "@bigcookie/mcp-nano-banana-image"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
